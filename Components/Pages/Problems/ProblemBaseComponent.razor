@typeparam TInputItem
@typeparam TReturnItem

@code {
    protected virtual TReturnItem? DoPartALogic(TInputItem item)
    {
        return default;
    }

    protected virtual TReturnItem? DoPartBLogic(TInputItem item)
    {
        return default;
    }
}
